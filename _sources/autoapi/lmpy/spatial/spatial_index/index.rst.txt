:py:mod:`lmpy.spatial.spatial_index`
====================================

.. py:module:: lmpy.spatial.spatial_index

.. autoapi-nested-parse::

   Module containing a class for working with a spatial index.

   Version 1: Store geometries in memory in table.  Save as wkt.



Module Contents
---------------

Classes
~~~~~~~

.. autoapisummary::

   lmpy.spatial.spatial_index.SpatialIndex




.. py:class:: SpatialIndex(index_name=None)

   Constructor for the spatial index.

   :param index_name: A name to use for saving the index to a file.
   :type index_name: str

   .. py:method:: _point_intersect(pt_x, pt_y, geom)
      :staticmethod:

      Perform a point intersect.

      :param pt_x: The x coordinate of the point.
      :type pt_x: numeric
      :param pt_y: The y coordinate of the point.
      :type pt_y: numeric
      :param geom: The geometry to intersect.
      :type geom: ogr.Geometry

      :returns: Indication if the point is within the geometry.
      :rtype: bool


   .. py:method:: add_feature(identifier, geom, att_dict)

      Add a feature to the index.

      :param identifier: An identifier for this feature in the lookup table.
      :type identifier: str
      :param geom: A geometry to spatially index.
      :type geom: ogr.Geometry
      :param att_dict: A dictionary of attributes to store in the lookup table.
      :type att_dict: dict


   .. py:method:: close()

      Close the index.


   .. py:method:: save()

      Save the index attributes.


   .. py:method:: search(x, y)

      Search for x, y and return attributes in lookup if found.

      :param x: The x coordinate to search for.
      :type x: numeric
      :param y: The y coordinate to search for.
      :type y: numeric

      :returns: A dictionary of index hits for the search.
      :rtype: dict



