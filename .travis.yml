dist: xenial
language: python
python:
  - "3.5"
  - "3.6"
  - "3.7"
install: "pip install -r requirements.txt"
script:
  - py.test tests/ --pep8 lmpy -v --cov lmpy --cov-report term-missing
after_success:
  - coveralls

